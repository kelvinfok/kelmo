# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

use_frameworks!
inhibit_all_warnings!

target 'kelmo' do
  
  pod 'SnapKit'
  pod 'Alamofire'
  pod 'SDWebImage', '~> 4.0'

end
